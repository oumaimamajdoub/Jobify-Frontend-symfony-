{% extends 'base.html.twig' %}

{% block title %}Document{% endblock %}

{% block body %}
    <h1>Document</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ document.id }}</td>
        </tr>
        <tr>
            <th>DateDeCreation</th>
            <td>{{ document.dateDeCreation ? document.dateDeCreation|date('Y-m-d') : '' }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ document.nom }}</td>
        </tr>
        <tr>
            <th>Path</th>
            <td>{{ document.path }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_document_index') }}">back to list</a>

    <a href="{{ path('app_document_edit', {'id': document.id}) }}">edit</a>

    {{ include('document/_delete_form.html.twig') }}
{% endblock %}
